# lambda in python are the elegant to create an on-line functions
# These function do not have name. So, they are also called an anonymous function

summ = lambda a, b: a + b
print(summ(4, 5))

is_even = lambda x: x % 2 == 0
print(is_even(4))
